# Project 0

ENGO 551 - Adv. Topics on Geospatial Technologies
Lab0 ~ Karoll Moyano
UCID 3011-3646

General topic: Website about me and some things that are important to me.

index.html:
    - Nav links at top bar as navigation links to each external .html.
    - Unordered list expressing multiple things that are important to me, picture as background and profile pic.
    - Bootstraps grid with three columns.
    - External stylesheet linked.
    - Mulitple CSS properties: 
        - background image, font families, padding, color, border radius, and box-shadow.
    - Use of class selectors:
        - .top-bar, .hero-section, .profile-pic, and .nav-links.


interest1.html:
    - Nav links at top bar as navigation links to each external .html.
    - Bootstraps grid with three columns
    - Picture as background.
    - Mulitple CSS properties: 
        - background image, font families, padding, color, border radius, and box-shadow.
    - Use of class selectors:
        - .top-bar, .hero-section, .nav-links, and .collage-bg.


interest2.html:
    - Nav links at top bar as navigation links to each external .html.
    - Multiple unordered lists and a plain list describing plant care requirements.
    - Picture as background and more included per plant.
    - Bootstrap 4 is included via CDN and used throughout the site.
    - Bootstrap grid with multple columns.
    - External stylesheet linked.
    - Use of class selectors:
        - .top-bar, .hero-text, .plant-pic, and .nav-links.


interest3.html:
    - Nav links at top bar as navigation links to each external .html.
    - Multiple unordered lists and a plain list describing plant care requirements.
    - Picture as background and more included per plant.
    - Bootstrap 4 is included via CDN and used throughout the site.
    - Bootstrap grid with multple columns.
    - External stylesheet linked.
   - Mulitple CSS properties: 
        - background-image, padding, color, font-size, border radius, and box-shadow.
    - Use of class selectors:
        - .top-bar, .hero-text, .hero-content,  .nav-links, .food-pic.